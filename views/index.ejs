<!DOCTYPE html>
<html>
<head>
  <title>Status do Bot Minecraft</title>
  <style>
    body {
      font-family: sans-serif;
      padding: 20px;
      background: #f0f0f0;
    }
    .box {
      background: #fff;
      padding: 20px;
      margin-bottom: 20px;
      border-radius: 8px;
    }
    ul {
      list-style: none;
      padding: 0;
    }
  </style>
</head>
<body>
  <h1>🤖 Status do Bot Minecraft</h1>

  <div class="box">
    <p>Status: <strong><%= status %></strong></p>
    <p>⏱ Uptime: <%= botUptime %> segundos</p>
    <p>👥 Jogadores online: <%= playersOnline %></p>
  </div>

  <div class="box">
    <h2>📜 Últimos comandos</h2>
    <ul>
      <% commands.forEach(cmd => { %>
        <li><strong><%= cmd.username %></strong>: <%= cmd.message %> (<%= new Date(cmd.timestamp).toLocaleTimeString() %>)</li>
      <% }) %>
    </ul>
  </div>
</body>
</html>
